//import all models and insert data into the database

// models
const db = require('../models');
const { Student, Programme, Semester, Course, StudentCourse, Transcript, AwardedDegree, Group, SemesterCourse } = require('../models');

const initialize = async () => {

};